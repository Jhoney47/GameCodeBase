# 游戏码宝 - 完整项目报告

**项目名称：** 游戏兑换码收集应用  
**版本：** 2.0.0  
**更新日期：** 2026-01-21  
**开发者：** Jhoney47

---

## 📋 项目概述

游戏码宝是一个专注于收集和分享游戏兑换码的移动应用，支持多个热门游戏，为玩家提供最新、最准确的兑换码信息。

### 核心特性

- 🌐 **国内无障碍访问** - 使用jsDelivr CDN，国内用户无需VPN即可使用
- 🎮 **动态游戏管理** - 游戏列表完全动态生成，无需修改前端代码
- 🔄 **实时数据同步** - 后台更新后自动同步到前端
- 📱 **跨平台支持** - Flutter开发，支持iOS、Android、Web
- 🎨 **精美UI设计** - 靛蓝色主题，符合游戏风格
- 🔍 **强大搜索筛选** - 支持多维度搜索、筛选、排序

---

## 🏗️ 技术架构

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Flutter | 3.0+ | 跨平台移动应用框架 |
| Dart | 3.0+ | 编程语言 |
| http | 1.1.0 | HTTP请求 |
| provider | 6.1.1 | 状态管理 |
| pull_to_refresh | 2.0.0 | 下拉刷新 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 18+ | 服务器运行环境 |
| TypeScript | 5.0+ | 编程语言 |
| tRPC | 11.7.2 | API框架 |
| Drizzle ORM | 0.44.7 | 数据库ORM |
| MySQL | 8.0+ | 数据库 |

### 数据流架构

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   Flutter   │      │   jsDelivr   │      │   GitHub    │
│   前端应用   │ ───> │   CDN加速    │ ───> │   JSON仓库  │
└─────────────┘      └──────────────┘      └─────────────┘
                                                   ▲
                                                   │
                                            自动导出JSON
                                                   │
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   Admin     │      │   tRPC API   │      │   MySQL     │
│   后台管理   │ ───> │   后端服务   │ ───> │   数据库    │
└─────────────┘      └──────────────┘      └─────────────┘
```

---

## 📱 前端功能详解

### 1. 首页（home_screen.dart）

**功能：**
- 动态游戏标签页（根据JSON自动生成）
- 下拉刷新获取最新数据
- 搜索兑换码
- 多维度筛选（游戏、类型、状态）
- 多种排序（最新、可信度、即将过期）

**关键实现：**
```dart
// 动态生成TabBar
_tabController = TabController(
  length: data.games.length + 1, // +1 for "全部" tab
  vsync: this,
);

TabBar(
  controller: _tabController,
  isScrollable: true,
  tabs: [
    const Tab(text: '全部'),
    ...data.games.map((game) => Tab(text: game.gameName)),
  ],
)
```

### 2. 兑换码卡片（code_card.dart）

**功能：**
- 显示兑换码详细信息
- 状态指示（有效/过期）
- 可信度进度条
- 触摸反馈

**状态逻辑：**
- `isActive == true` → 绿色高亮，正常透明度
- `isActive == false` → 灰色置灰，降低透明度

### 3. 详情页（code_detail_screen.dart）

**功能：**
- 完整兑换码信息展示
- 一键复制兑换码
- 奖励内容说明
- 来源和有效期信息

### 4. 数据获取（code_repository.dart）

**核心特性：**
- 使用jsDelivr CDN加速GitHub访问
- 添加时间戳参数破除CDN缓存
- 国内用户无需VPN即可访问

```dart
static const String _baseUrl =
    'https://cdn.jsdelivr.net/gh/Jhoney47/GameCodeBase@main/GameCodeBase.json';

Future<GameCodeResponse> fetchGameCodes() async {
  // 添加时间戳破除CDN缓存
  final timestamp = DateTime.now().millisecondsSinceEpoch;
  final url = Uri.parse('$_baseUrl?v=$timestamp');
  
  final response = await http.get(url, headers: {
    'Cache-Control': 'no-cache',
  });
  
  return GameCodeResponse.fromJson(json.decode(response.body));
}
```

---

## 🔧 后台功能详解

### 1. 管理后台（Dashboard.tsx）

**功能：**
- 兑换码CRUD操作
- 发布状态管理
- 审核用户提交的兑换码
- 批量操作
- 定时任务管理

### 2. 数据导出（export_codes_to_json.ts）

**核心改进：**
- 预定义游戏列表，防止游戏类消失
- 只导出已发布且有效的兑换码
- 添加 `reviewStatus` 字段
- 自动提交到GitHub

**关键代码：**
```typescript
// 预定义游戏列表（即使没有兑换码也保留）
const predefinedGames = ['铃兰之剑', '杖剑传说', '植物大战僵尸2'];

// 初始化游戏分组
const gameGroups: Record<string, any[]> = {};
predefinedGames.forEach(game => {
  gameGroups[game] = [];
});
```

### 3. API路由（server/routers.ts）

**核心改进：**
- 所有修改数据的操作后自动导出JSON
- 批量删除限制（最多20条）
- 完善的错误处理

**自动导出触发点：**
- 删除兑换码
- 更新发布状态
- 批量操作
- 审核通过
- 爬虫刷新

---

## 🐛 Bug修复记录

### Bug #1: 删除兑换码误删整个游戏类 ✅

**问题：** 删除某个游戏的最后一条兑换码时，该游戏类在JSON中消失

**原因：** 导出脚本动态从兑换码中提取游戏列表，如果某个游戏没有兑换码，就不会出现在JSON中

**修复：** 预定义游戏列表，即使没有兑换码也保留游戏类

### Bug #2: 导出未过滤数据 ✅

**问题：** 导出所有兑换码，包括未发布和已过期的

**修复：** 使用 `getActiveRedemptionCodes()` 只导出有效兑换码

### Bug #3: 缺少自动导出机制 ✅

**问题：** 后台操作后需要手动运行导出脚本

**修复：** 在所有修改数据的操作后自动调用 `exportCodesToJSON()`

### Bug #4: API路由命名不一致 ✅

**问题：** 前端调用 `/api/admin/delete`，但tRPC路由是 `deleteCode`

**修复：** 统一使用tRPC客户端调用

### Bug #5: 批量删除无限制 ✅

**问题：** 批量删除没有数量限制，容易误操作

**修复：** 添加限制，一次最多删除20条

### Bug #6: 缺少reviewStatus字段 ✅

**问题：** 导出的JSON缺少 `reviewStatus` 字段，导致Flutter前端崩溃

**修复：** 在导出时添加该字段

---

## 📂 项目结构

```
GameCodeBase/
├── flutter_app/                    # Flutter前端应用
│   ├── lib/
│   │   ├── main.dart              # 应用入口
│   │   ├── models/
│   │   │   ├── game_code.dart     # 数据模型
│   │   │   └── game_code.g.dart   # JSON序列化
│   │   ├── repositories/
│   │   │   └── code_repository.dart # CDN数据访问
│   │   ├── screens/
│   │   │   ├── home_screen.dart   # 首页
│   │   │   └── code_detail_screen.dart # 详情页
│   │   ├── widgets/
│   │   │   └── code_card.dart     # 兑换码卡片
│   │   └── theme/
│   │       └── app_theme.dart     # 主题配置
│   ├── pubspec.yaml               # 依赖配置
│   ├── README.md                  # Flutter项目文档
│   └── QUICKSTART.md              # 快速开始指南
│
├── admin_backend/                  # 后台管理系统
│   ├── server/
│   │   ├── routers.ts             # API路由（已修复）
│   │   └── db.ts                  # 数据库操作
│   ├── export_codes_to_json.ts    # 导出脚本（已修复）
│   └── BUG_FIX_GUIDE.md           # Bug修复指南
│
├── GameCodeBase.json               # 兑换码数据文件
├── PROJECT_REPORT.md               # 项目报告（本文件）
└── README.md                       # 仓库说明
```

---

## 🚀 快速开始

### 前端部署

#### 1. 安装Flutter环境

```bash
# macOS
brew install flutter

# Windows
# 下载Flutter SDK: https://docs.flutter.dev/get-started/install
```

#### 2. 运行应用

```bash
cd flutter_app
flutter pub get
flutter run
```

#### 3. 打包APK

```bash
flutter build apk --release
```

生成的APK位于：`build/app/outputs/flutter-apk/app-release.apk`

### 后台部署

#### 1. 安装依赖

```bash
cd admin_backend
pnpm install
```

#### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 文件，配置数据库连接
```

#### 3. 运行服务

```bash
pnpm dev
```

#### 4. 导出JSON到GitHub

```bash
pnpm export
```

---

## 🌐 CDN配置

### 当前使用的CDN

```
https://cdn.jsdelivr.net/gh/Jhoney47/GameCodeBase@main/GameCodeBase.json
```

### 备选CDN

```
# Statically CDN
https://cdn.statically.io/gh/Jhoney47/GameCodeBase/main/GameCodeBase.json

# ghproxy.com
https://ghproxy.com/https://raw.githubusercontent.com/Jhoney47/GameCodeBase/main/GameCodeBase.json
```

### 切换CDN

修改 `flutter_app/lib/repositories/code_repository.dart` 中的 `_baseUrl` 常量即可。

---

## 📊 数据格式

### JSON结构

```json
{
  "version": "2.0.1",
  "lastUpdated": "2026-01-21T10:30:00.000Z",
  "totalCodes": 15,
  "games": [
    {
      "gameName": "铃兰之剑",
      "codeCount": 5,
      "codes": [
        {
          "code": "KSTGIFT",
          "rewardDescription": "金币*1000",
          "sourcePlatform": "TapTap论坛",
          "sourceUrl": "https://...",
          "expireDate": "2026-01-30T00:00:00.000Z",
          "status": "active",
          "codeType": "permanent",
          "publishDate": "2026-01-18T00:00:00.000Z",
          "verificationCount": 10,
          "reviewStatus": "approved"
        }
      ]
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 兑换码 |
| rewardDescription | string | 奖励描述 |
| sourcePlatform | string | 来源平台 |
| sourceUrl | string? | 来源链接 |
| expireDate | string? | 过期时间（ISO 8601） |
| status | enum | 状态：active/expired/invalid |
| codeType | enum | 类型：permanent/limited |
| publishDate | string? | 发布时间（ISO 8601） |
| verificationCount | number | 验证次数 |
| reviewStatus | string | 审核状态：approved/pending/rejected |

---

## 🔄 更新流程

### 添加新兑换码

1. 在后台管理系统添加兑换码
2. 系统自动导出JSON到GitHub
3. jsDelivr CDN自动同步
4. 用户下拉刷新获取最新数据

### 添加新游戏

1. 在后台添加新游戏的兑换码
2. 导出脚本自动识别新游戏
3. 前端自动生成新游戏的Tab
4. 无需修改任何代码

---

## 📈 性能优化

### 前端优化

- 使用 `FlatList` 替代 `ScrollView` + `.map()`
- 图片懒加载
- 本地缓存（SharedPreferences）
- 下拉刷新防抖

### 后端优化

- 数据库索引优化
- API响应缓存
- 批量操作事务处理
- 定时任务异步执行

### CDN优化

- jsDelivr全球加速
- 时间戳参数破除缓存
- Gzip压缩
- HTTP/2支持

---

## 🔒 安全措施

### 前端安全

- HTTPS加密传输
- 输入验证和清理
- XSS防护

### 后端安全

- SQL注入防护（Drizzle ORM）
- CSRF防护
- 批量操作限制
- 审核机制

---

## 📱 用户使用指南

### 安装应用

**Android用户：**
1. 下载APK文件
2. 允许"未知来源"安装
3. 安装并打开应用

**iOS用户：**
1. 通过TestFlight安装（内测）
2. 或从App Store下载（正式版）

### 使用应用

1. **浏览兑换码** - 在首页查看所有游戏的兑换码
2. **切换游戏** - 点击顶部游戏标签切换
3. **搜索** - 在搜索框输入游戏名称或兑换码
4. **筛选** - 点击筛选按钮选择类型和状态
5. **排序** - 选择排序方式（最新、可信度、即将过期）
6. **复制兑换码** - 点击兑换码卡片进入详情页，点击复制按钮
7. **刷新** - 下拉刷新获取最新数据

---

## 🎯 未来规划

### 短期计划（1-3个月）

- [ ] 添加收藏功能
- [ ] 添加历史记录
- [ ] 优化游戏图标
- [ ] 添加推送通知

### 中期计划（3-6个月）

- [ ] 用户账号系统
- [ ] 社区评论功能
- [ ] 兑换码有效性投票
- [ ] 多语言支持

### 长期计划（6-12个月）

- [ ] AI智能推荐
- [ ] 游戏资讯聚合
- [ ] 跨平台数据同步
- [ ] 开放API接口

---

## 🤝 贡献指南

欢迎贡献代码、报告Bug或提出建议！

### 如何贡献

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

### 代码规范

- **Flutter代码** - 遵循Dart官方风格指南
- **TypeScript代码** - 使用ESLint配置
- **提交信息** - 使用语义化提交规范

---

## 📞 联系方式

- **GitHub:** https://github.com/Jhoney47
- **仓库地址:** https://github.com/Jhoney47/GameCodeBase
- **问题反馈:** https://github.com/Jhoney47/GameCodeBase/issues

---

## 📄 许可证

MIT License

---

## 🙏 致谢

感谢以下开源项目和服务：

- [Flutter](https://flutter.dev/) - 跨平台移动应用框架
- [jsDelivr](https://www.jsdelivr.com/) - 免费CDN服务
- [GitHub](https://github.com/) - 代码托管和数据存储
- [tRPC](https://trpc.io/) - 类型安全的API框架
- [Drizzle ORM](https://orm.drizzle.team/) - 轻量级ORM

---

## 📝 更新日志

### v2.0.1 (2026-01-21)

**新增：**
- ✅ Flutter前端应用
- ✅ jsDelivr CDN加速
- ✅ 动态游戏标签页
- ✅ 下拉刷新功能

**修复：**
- ✅ 删除兑换码误删游戏类
- ✅ 缺少自动导出机制
- ✅ 缺少reviewStatus字段
- ✅ 导出未过滤数据
- ✅ 批量删除无限制
- ✅ API路由命名不一致

**优化：**
- ✅ 响应式布局
- ✅ 触摸反馈
- ✅ 错误处理
- ✅ 加载状态

### v1.0.0 (2026-01-01)

- 🎉 初始版本发布

---

**最后更新：** 2026-01-21  
**文档版本：** 2.0.1
